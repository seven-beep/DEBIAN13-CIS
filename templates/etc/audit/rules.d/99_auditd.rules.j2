{{ file_managed_by_ansible }}
### YOUR CHANGES WILL BE LOST!

# This template will set all of the auditd configurations via a handler in the role in one task instead of individually
{% if deb13cis_rule_6_2_3_1 %}
-a always,exit -F arch=b64 -S all -F path=/etc/sudoers -F perm=wa -k scope
-a always,exit -F arch=b64 -S all -F dir=/etc/sudoers.d -F perm=wa -k scope
{% endif %}
{% if deb13cis_rule_6_2_3_2 %}
{% set syscalls = ["execve"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -C euid!=uid -F auid!=unset -S {{ arch_syscalls|join(',') }} -k user_emulation
{% endif %}
{% if deb13cis_rule_6_2_3_3 %}
-a always,exit -F arch=b64 -S all -F path=/var/log/sudo.log -F perm=wa -k sudo_log_file
{% endif %}
{% if deb13cis_rule_6_2_3_4 %}
{% set syscalls = ["adjtimex","settimeofday"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -k time-change
-a always,exit -F arch=b64 -S clock_settime -F a0=0x0 -k time-change
{% endif %}
{% if deb13cis_rule_6_2_3_5 %}
{% set syscalls = ["sethostname","setdomainname"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -k system-locale
{% endif %}
{% if deb13cis_rule_6_2_3_6 %}
-a always,exit -F arch=b64 -S all -F path=/etc/issue -F perm=wa -k system-locale
-a always,exit -F arch=b64 -S all -F path=/etc/issue.net -F perm=wa -k system-locale
{% endif %}
{% if deb13cis_rule_6_2_3_7 %}
-a always,exit -F arch=b64 -S all -F path=/etc/hosts -F perm=wa -k system-locale
-a always,exit -F arch=b64 -S all -F path=/etc/hostname -F perm=wa -k system-locale
{% endif %}
{% if deb13cis_rule_6_2_3_8 %}
{% if discovered_interfaces_stat.stat.exists %}
-a always,exit -F arch=b64 -S all -F path=/etc/network/interfaces -F perm=wa -k system-locale
{% else %}
## The following line is commented out as path not present when checked
# -a always,exit -F arch=b64 -S all -F path=/etc/network/interfaces -F perm=wa -k system-locale
{% endif %}
{% if discovered_interfaces_d_stat.stat.exists %}
-a always,exit -F arch=b64 -S all -F dir=/etc/network/interfaces.d -F perm=wa -k system-locale
{% else %}
## The following line is commented out as path not present when checked
# -a always,exit -F arch=b64 -S all -F dir=/etc/network/interfaces.d -F perm=wa -k system-locale
{% endif %}
{% if discovered_netplan_stat.stat.exists %}
-a always,exit -F arch=b64 -S all -F dir=/etc/netplan/ -F perm=wa -k system-locale
{% else %}
## The following line is commented out as path not present when checked
# -a always,exit -F arch=b64 -S all -F dir=/etc/netplan -F perm=wa -k system-locale
{% endif %}
{% if 'systemd-networkd' in ansible_facts.packages %}
-a always,exit -F arch=b64 -S all -F path=/etc/systemd/networkd.conf -F perm=wa -k system-locale
-a always,exit -F arch=b64 -S all -F dir=/etc/systemd/network -F perm=wa -k system-locale
{% else %}
## The following line is commented out as systemd-networkd was nost installed when checked
# -a always,exit -F arch=b64 -S all -F path=/etc/systemd/networkd.conf -F perm=wa -k system-locale
# -a always,exit -F arch=b64 -S all -F dir=/etc/systemd/network -F perm=wa -k system-locale
{% endif %}
{% endif %}
{% if deb13cis_rule_6_2_3_9 %}
{% if discovered_network_manager_stat.stat.exists %}
-a always,exit -F arch=b64 -S all -F dir=/etc/NetworkManager/ -F perm=wa -k system-locale
{% else %}
## The following line is commented out as path not present when checked
# -a always,exit -F arch=b64 -S all -F dir=/etc/NetworkManager/ -F perm=wa -k system-locale
{% endif %}
{% endif %}
{% if deb13cis_rule_6_2_3_10 %}
{% if discovered_privilege_processes is defined %}
{% for proc in discovered_privilege_processes.stdout_lines -%}
-a always,exit -F path={{ proc }} -F perm=x -F auid>=1000 -F auid!=unset -k privileged
{% endfor %}
{% endif %}
{% endif %}
{% if deb13cis_rule_6_2_3_11 %}
{% set syscalls = ["creat","open","openat","truncate","ftruncate"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F exit=-EACCES -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k access
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F exit=-EPERM -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k access
{% endif %}
{% if deb13cis_rule_6_2_3_12 %}
-a always,exit -F arch=b64 -S all -F path=/etc/group -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_13 %}
-a always,exit -F arch=b64 -S all -F path=/etc/passwd -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_14 %}
-a always,exit -F arch=b64 -S all -F path=/etc/gshadow -F perm=wa -k identity
-a always,exit -F arch=b64 -S all -F path=/etc/shadow -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_15 %}
-a always,exit -F arch=b64 -S all -F path=/etc/security/opasswd -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_16 %}
-a always,exit -F arch=b64 -S all -F path=/etc/nsswitch.conf -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_17 %}
-a always,exit -F arch=b64 -S all -F path=/etc/pam.conf -F perm=wa -k identity
-a always,exit -F arch=b64 -S all -F dir=/etc/pam.d -F perm=wa -k identity
{% endif %}
{% if deb13cis_rule_6_2_3_18 %}
{% set syscalls = ["chmod","fchmod","fchmodat","fchmodat2"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_mod
{% endif %}
{% if deb13cis_rule_6_2_3_19 %}
{% set syscalls = syscalls | select("in", supported_syscalls) | list %}
{% set arch_syscalls = ["chown","fchown","lchown","fchownat"] %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_mod
{% endif %}
{% if deb13cis_rule_6_2_3_20 %}
{% set syscalls = ["setxattr","lsetxattr","fsetxattr","removexattr","lremovexattr","fremovexattr"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_mod
{% endif %}
{% if deb13cis_rule_6_2_3_21 %}
{% set syscalls = ["mount"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k mounts
{% endif %}
{% if deb13cis_rule_6_2_3_22 %}
-a always,exit -F arch=b64 -S all -F path=/var/run/utmp -F perm=wa -k session
-a always,exit -F arch=b64 -S all -F path=/var/log/wtmp -F perm=wa -k session
-a always,exit -F arch=b64 -S all -F path=/var/log/btmp -F perm=wa -k session
{% endif %}
{% if deb13cis_rule_6_2_3_23 %}
-a always,exit -F arch=b64 -S all -F path=/var/log/lastlog -F perm=wa -k logins
-a always,exit -F arch=b64 -S all -F path=/var/run/faillock -F perm=wa -k logins
{% endif %}
{% if deb13cis_rule_6_2_3_24 %}
{% set syscalls = ["unlink","unlinkat"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k delete
{% endif %}
{% if deb13cis_rule_6_2_3_25 %}
{% set syscalls = ["rename","renameat","renameat2"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k delete
{% endif %}
{% if deb13cis_rule_6_2_3_26 %}
-a always,exit -F arch=b64 -S all -F path=/etc/apparmor -F perm=wa -k MAC-policy
-a always,exit -F arch=b64 -S all -F dir=/etc/apparmor.d -F perm=wa -k MAC-policy
{% endif %}
{% if deb13cis_rule_6_2_3_27 %}
-a always,exit -F arch=b64 -S all -F path=/usr/bin/chcon -F perm=x -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_chng
{% endif %}
{% if deb13cis_rule_6_2_3_28 %}
-a always,exit -F arch=b64 -S all -F path=/usr/bin/setfacl -F perm=x -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_chng
{% endif %}
{% if deb13cis_rule_6_2_3_29 %}
-a always,exit -F arch=b64 -S all -F path=/usr/bin/chacl -F perm=x -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k perm_chng
{% endif %}
{% if deb13cis_rule_6_2_3_30 %}
-a always,exit -F arch=b64 -S all -F path=/usr/sbin/usermod -F perm=x -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k usermod
{% endif %}
{% if deb13cis_rule_6_2_3_31 %}
-a always,exit -F arch=b64 -S all -F path=/usr/bin/kmod -F perm=x -F auid>={{ prelim_min_int_uid }} -F auid!=unset -k kernel_modules
{% endif %}
{% if deb13cis_rule_6_2_3_32 %}
{% set syscalls = ["init_module","finit_module"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=-1 -k kernel_modules
{% endif %}
{% if deb13cis_rule_6_2_3_33 %}
{% set syscalls = ["delete_module"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=-1 -k kernel_modules
{% endif %}
{% if deb13cis_rule_6_2_3_34 %}
{% set syscalls = ["create_module","query_module"] %}
{% set arch_syscalls = syscalls | select("in", supported_syscalls) | list %}
-a always,exit -F arch=b64 -S {{ arch_syscalls|join(',') }} -F auid>={{ prelim_min_int_uid }} -F auid!=-1 -k kernel_modules
{% endif %}
{% if deb13cis_rule_6_2_3_35 %}
-c
{% endif %}
{% if deb13cis_rule_6_2_3_36 %}
-e 2
{% endif %}
